//FAVORITOS (Tela de notícias favoritas)
import { Text, View } from "react-native";

export default function Favoritos() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Notícias Favoritas</Text>
    </View>
  );
}
